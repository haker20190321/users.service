version: '3.0'
services:
  postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_PASSWORD: test_user
      POSTGRES_USER: test_user
      POSTGRES_DB: users
    ports:
      - 5432:5432